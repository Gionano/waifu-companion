# Taruh file VRM kamu di folder ini

Letakkan model kamu sebagai:

    avatar.vrm

File `.vrm` tidak di-commit / tidak disertakan — ini hanya placeholder folder.
Kalau mau pakai nama lain, ubah path di `src/main.js` (`'/models/avatar.vrm'`).
